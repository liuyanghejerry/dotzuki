node_modules/
dist/
dist-electron/
release/
tsconfig.tsbuildinfo
pnpm-lock.yaml
test-project/

# Playwright E2E artifacts (see playwright.config.ts; fixture lives in e2e/fixtures/)
e2e/.scratch/
test-results/
playwright-report/
blob-report/

# AI provider secrets — never commit. API keys live in the browser (localStorage);
# this file, if ever written, may hold provider config and must stay local.
.dotzuki-editor.providers.local.json
**/.dotzuki-editor.providers.local.json
